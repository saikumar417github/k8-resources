apiVersion: v1
kind: Pod
metadata:
  name: podconfig
  namespace: expense
  labels:
    project: expense
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - secretRef:
        name: pod.secret